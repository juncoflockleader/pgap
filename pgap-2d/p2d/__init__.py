"""p2d — Procedural 2D Asset Pipeline (portraits + battle backdrops).

Deterministic, offline, dependency-light (pure Python + numpy). Same
(spec, seed) -> byte-identical PNG on any machine.
"""

__version__ = "0.1.0"
